[defaults]
inventory = inventory.ini
host_key_checking = False
# Project-local collections path: collections committed here override
# anything installed in ~/.ansible/collections or system-wide.
collections_path = ./collections
