# Single-host inventory: run the controller and the managed host as the same box.
# Use this when you have one machine and just want to learn the playbooks.
[docker_hosts]
localhost ansible_connection=local

[docker_hosts:vars]
ansible_python_interpreter=/usr/bin/python3
