---
- name: Restart docker
  ansible.builtin.systemd:
    name: docker
    state: restarted
    daemon_reload: true

- name: Load xt_addrtype
  community.general.modprobe:
    name: xt_addrtype
    state: present
    persistent: present
